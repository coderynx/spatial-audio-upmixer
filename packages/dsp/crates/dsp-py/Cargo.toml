[package]
name = "upmixer-dsp-py"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Python bindings for the upmixer shared DSP core"

[lib]
name = "upmixer_dsp"
crate-type = ["cdylib"]

[dependencies]
numpy = "0.29.0"
pyo3 = { version = "0.29.2", features = ["extension-module"] }
upmixer-dsp-core.workspace = true
