import * as React from "react";
import { Card, CardContent } from "upmixer-web";

export const InCard = () => (
  <Card className="w-72">
    <CardContent className="pt-3 text-sm text-muted-foreground">
      Upmixed to 7.1.4 with stem separation, mastered to −14 LUFS with
      true-peak limiting at −1 dBTP.
    </CardContent>
  </Card>
);
