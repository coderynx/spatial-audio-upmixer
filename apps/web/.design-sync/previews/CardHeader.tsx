import * as React from "react";
import { Card, CardHeader, CardTitle, CardDescription } from "upmixer-web";

export const InCard = () => (
  <Card className="w-72">
    <CardHeader>
      <CardTitle>Ambient Session</CardTitle>
      <CardDescription>Stereo · 44.1 kHz · 3:42</CardDescription>
    </CardHeader>
  </Card>
);
