"""Infrastructure shared across `upmixer_web` feature slices: ORM models, the
database engine, object storage, the `upmixer` core boundary, and metadata
extraction. See `docs/web_api_architecture.md`."""
